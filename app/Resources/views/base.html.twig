<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
			<link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" type="text/css" />
	{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="container">
            <div id="header" class="hero-unit">
                <h1>Welcome</h1>
                <p>Page de tests</p>
            </div>
            <div class="row">
                    <div id="menu" class="span3">
                            <h3>Test event</h3>
                            {% render "SrsEventBundle:Event:menu" %}
                            <h3>Test news</h3>
                            <ul class="nav nav-pills nav-stacked">
                                    <li><a href="{{ path('srs_news_index') }}">Liste des News</a></li>
                                    <li><a href="{{ path('srs_news_add') }}">Ajout d'une News</a></li>
                            </ul>
                    </div>
                    
                    <div id="content" class="span9">
                            {% block body %}
                            {% endblock %}
                    </div>
            </div>

            <hr>

            <footer>
                    <p>The sky's the limit &copy; 2012 and beyond.</p>
            </footer>
        </div>
        {% block javascripts %}
		<script type="text/javascript" src="{{ asset('js/bootstrap.js') }}"></script>
	{% endblock %}
    </body>
</html>
